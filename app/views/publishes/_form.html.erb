<%= form_for(@publish) do |f| %>
  <% if @publish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@publish.errors.count, "error") %> prohibited this publish from being saved:</h2>

      <ul>
      <% @publish.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :publishTitle %><br>
    <%= f.text_field :publishTitle %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
